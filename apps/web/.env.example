# Local dev: backend URL for Vite dev server proxy (see vite.config.ts).
# Leave unset when building for same-origin deploy — the app uses relative /api and /socket.io.
VITE_API_URL=http://localhost:3080
